{
  "name": "persian-captcha-web",
  "version": "0.1.0",
  "private": true,
  "description": "Browser assets for the persian-captcha service. Built output is committed to dist/ so that `go build` alone produces a working binary.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
